apiVersion: v1
kind: Service
metadata:
  name: ids
  labels:
    app: ids-label
    service: ids
spec:
  type: NodePort
  selector:
    app: ids-label
    service: ids
  ports:
    - name: http
      protocol: TCP
      port: 1111
      targetPort: 8080
