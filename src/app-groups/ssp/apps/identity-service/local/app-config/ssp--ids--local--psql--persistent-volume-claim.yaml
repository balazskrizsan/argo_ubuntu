apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ssp--ids--local--psql--persistent-volume-claim
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  volumeName: ssp--ids--local--psql--persistent-volume
  resources:
    requests:
      storage: 10Gi
