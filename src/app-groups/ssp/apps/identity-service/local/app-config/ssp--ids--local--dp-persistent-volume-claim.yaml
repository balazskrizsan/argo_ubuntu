apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ssp--ids--local--dp-persistent-volume-claim
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  volumeName: ssp--ids--local--dp-persistent-volume
  resources:
    requests:
      storage: 1Gi
