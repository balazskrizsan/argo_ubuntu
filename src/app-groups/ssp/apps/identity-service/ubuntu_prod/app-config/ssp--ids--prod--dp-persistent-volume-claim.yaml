apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ssp--ids--prod--dp-persistent-volume-claim
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  resources:
    requests:
      storage: 1Gi
