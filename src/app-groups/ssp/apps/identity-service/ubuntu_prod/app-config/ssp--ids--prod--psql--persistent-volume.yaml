apiVersion: v1
kind: PersistentVolume
metadata:
  name: ssp--ids--prod--psql--persistent-volume
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/ssp/ssp__ids__local__psql__persistent_volume"
