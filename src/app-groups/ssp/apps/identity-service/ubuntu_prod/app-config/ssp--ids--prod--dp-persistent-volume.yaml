apiVersion: v1
kind: PersistentVolume
metadata:
  name: ssp--ids--prod--dp-persistent-volume
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  hostPath:
    path: /mnt/k8s/ids-dpkeys