apiVersion: v1
kind: PersistentVolume
metadata:
  name: ssp--ids--local--dp-persistent-volume
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  hostPath:
    path: /mnt/k8s/ids-dpkeys