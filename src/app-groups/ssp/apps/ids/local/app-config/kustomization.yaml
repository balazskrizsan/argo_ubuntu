apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ssp-local

resources:
  - ssp--ids--local--psql--persistent-volume.yaml
  - ssp--ids--local--psql--persistent-volume-claim.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: ids-config
    namespace: ssp-local
    literals:
      - Certificate__Password=password
      - IdentityServer__IssuerUri=https://ids--smartscrumpoker.localhost.balazskrizsan.com/
      - ConnectionStrings__DefaultConnection="Host=ids-psql-service;Port=4053;Database=app_db;Username=admin;Password=password"
      - IdentityServer__Port=8080
      - Certificate__Path=keystore/certificate.pfx
      - IdentityServer__FrontendCorsOrigin=https://localhost.balazskrizsan.com:3010
      - IdentityServer__FrontendRedirectUri=https://localhost.balazskrizsan.com:3010/auth-callback
