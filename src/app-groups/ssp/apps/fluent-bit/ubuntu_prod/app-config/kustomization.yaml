apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ssp-prod

#todo: is that file needed?

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: fluent-bit-env
    namespace: ssp-prod
    literals:
      - env=PROD
      - logstash_host=logstash.elk.svc.cluster.prod
      - logstash_port=5044
