apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ssp--backend--local--psql--persistent-volume-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
