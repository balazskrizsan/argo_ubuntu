apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend-label
    service: backend
spec:
  type: NodePort
  selector:
    app: backend-label
    service: backend
  ports:
    - name: http
      protocol: TCP
      port: 1111
      targetPort: 8080
