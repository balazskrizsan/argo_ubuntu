apiVersion: v1
kind: Service
metadata:
  name: aws-services
  labels:
    app: aws-services
spec:
  type: NodePort
  selector:
    app: aws-services
    service: aws-services
  ports:
    - name: http
      protocol: TCP
      port: 1111
      targetPort: 8080
