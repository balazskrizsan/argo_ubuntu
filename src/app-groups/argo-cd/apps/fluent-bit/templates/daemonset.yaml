apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: argo-cd-fluent-bit
  labels:
    app: argo-cd-fluent-bit
spec:
  selector:
    matchLabels:
      app: argo-cd-fluent-bit
  template:
    metadata:
      labels:
        app: argo-cd-fluent-bit
    spec:
      serviceAccountName: argo-cd-fluent-bit
      initContainers:
      - name: config-subst
        image: alpine:3.18
        command:
        - /bin/sh
        - -c
        - |
          export ENV=$(cat /env-config/env)
          export APP=$(cat /env-config/app)
          export LOGSTASH_HOST=$(cat /env-config/logstash_host)
          export LOGSTASH_PORT=$(cat /env-config/logstash_port)

          echo "ENV: $ENV"
          echo "APP: $APP"
          echo "LOGSTASH_HOST: $LOGSTASH_HOST"
          echo "LOGSTASH_PORT: $LOGSTASH_PORT"
          
          
          cp /fluent-bit-configmap-source/fluent-bit.conf /fluent-bit-configmap/fluent-bit.conf
          cp /fluent-bit-configmap-source/parsers.conf /fluent-bit-configmap/parsers.conf

          echo "/fluent-bit-configmap/fluent-bit.conf:"
          echo "====================="
          cat /fluent-bit-configmap/fluent-bit.conf
          echo "====================="
          echo "/fluent-bit-configmap/parsers.conf:"
          echo "====================="
          cat /fluent-bit-configmap/parsers.conf
          echo "====================="

          sed -e "s/@ENV@/$ENV/g" \
              -e "s/@APP@/$APP/g" \
              -e "s/@LOGSTASH_HOST@/$LOGSTASH_HOST/g" \
              -e "s/@LOGSTASH_PORT@/$LOGSTASH_PORT/g" \
              /fluent-bit-configmap/fluent-bit.conf > /fluent-bit-configmap/fluent-bit.conf.tmp
          mv /fluent-bit-configmap/fluent-bit.conf.tmp /fluent-bit-configmap/fluent-bit.conf
        volumeMounts:
        - name: fluent-bit-configmap-source
          mountPath: /fluent-bit-configmap-source
          readOnly: true
        - name: fluent-bit-configmap
          mountPath: /fluent-bit-configmap
        - name: fluent-bit-env
          mountPath: /env-config
      containers:
      - name: argo-cd-fluent-bit
        image: fluent/fluent-bit:2.2.0
        imagePullPolicy: Always
        volumeMounts:
        - name: varlog
          mountPath: /var/log
        - name: varlibdockercontainers
          mountPath: /var/lib/docker/containers
          readOnly: true
        - name: fluent-bit-configmap
          mountPath: /fluent-bit/etc/
        resources:
          limits:
            memory: 200Mi
          requests:
            cpu: 100m
            memory: 100Mi
      volumes:
      - name: varlog
        hostPath:
          path: /var/log
      - name: varlibdockercontainers
        hostPath:
          path: /var/lib/docker/containers
      - name: fluent-bit-configmap-source
        configMap:
          name: argo-cd-fluent-bit-configmap
      - name: fluent-bit-configmap
        emptyDir: {}
      - name: fluent-bit-env
        configMap:
          name: argo-cd-fluent-bit-env
