apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: fluent-bit
  labels:
    app: fluent-bit
spec:
  selector:
    matchLabels:
      app: fluent-bit
  template:
    metadata:
      labels:
        app: fluent-bit
    spec:
      serviceAccountName: fluent-bit
      initContainers:
      - name: config-subst
        image: alpine:3.18
        command:
        - /bin/sh
        - -c
        - |
          export ENV=$(cat /env-config/env)
          export LOGSTASH_HOST=$(cat /env-config/logstash_host)
          export LOGSTASH_PORT=$(cat /env-config/logstash_port)

          echo "ENV: $ENV"
          echo "LOGSTASH_HOST: $LOGSTASH_HOST"
          echo "LOGSTASH_PORT: $LOGSTASH_PORT"
          
          
          cp /argo-cd-fluent-bit-filebased-configmap-source/fluent-bit.conf /argo-cd-fluent-bit-filebased-configmap/fluent-bit.conf
          cp /argo-cd-fluent-bit-filebased-configmap-source/parsers.conf /argo-cd-fluent-bit-filebased-configmap/parsers.conf

          echo "/argo-cd-fluent-bit-filebased-configmap/fluent-bit.conf:"
          echo "====================="
          cat /argo-cd-fluent-bit-filebased-configmap/fluent-bit.conf
          echo "====================="
          echo "/argo-cd-fluent-bit-filebased-configmap/parsers.conf:"
          echo "====================="
          cat /argo-cd-fluent-bit-filebased-configmap/parsers.conf
          echo "====================="

          sed -e "s/@ENV@/$ENV/g" \
              -e "s/@LOGSTASH_HOST@/$LOGSTASH_HOST/g" \
              -e "s/@LOGSTASH_PORT@/$LOGSTASH_PORT/g" \
              /argo-cd-fluent-bit-filebased-configmap/fluent-bit.conf > /argo-cd-fluent-bit-filebased-configmap/fluent-bit.conf.tmp
          mv /argo-cd-fluent-bit-filebased-configmap/fluent-bit.conf.tmp /argo-cd-fluent-bit-filebased-configmap/fluent-bit.conf
        volumeMounts:
        - name: argo-cd-fluent-bit-filebased-configmap-source
          mountPath: /argo-cd-fluent-bit-filebased-configmap-source
          readOnly: true
        - name: argo-cd-fluent-bit-filebased-configmap
          mountPath: /argo-cd-fluent-bit-filebased-configmap
        - name: fluent-bit-env
          mountPath: /env-config
      containers:
      - name: fluent-bit
        image: fluent/fluent-bit:2.2.0
        imagePullPolicy: Always
        volumeMounts:
        - name: varlog
          mountPath: /var/log
        - name: varlibdockercontainers
          mountPath: /var/lib/docker/containers
          readOnly: true
        - name: argo-cd-fluent-bit-filebased-configmap
          mountPath: /fluent-bit/etc/
        resources:
          limits:
            memory: 200Mi
          requests:
            cpu: 100m
            memory: 100Mi
      volumes:
      - name: varlog
        hostPath:
          path: /var/log
      - name: varlibdockercontainers
        hostPath:
          path: /var/lib/docker/containers
      - name: argo-cd-fluent-bit-filebased-configmap-source
        configMap:
          name: argo-cd-fluent-bit-filebased-configmap
      - name: argo-cd-fluent-bit-filebased-configmap
        emptyDir: {}
      - name: fluent-bit-env
        configMap:
          name: fluent-bit-env
