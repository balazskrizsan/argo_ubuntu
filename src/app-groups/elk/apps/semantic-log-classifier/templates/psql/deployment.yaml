apiVersion: apps/v1
kind: Deployment
metadata:
  name: semantic-log-classifier-psql
  labels:
    app: semantic-log-classifier-psql-label
spec:
  replicas: 1
  selector:
    matchLabels:
      app: semantic-log-classifier-psql-label
  template:
    metadata:
      labels:
        app: semantic-log-classifier-psql-label
    spec:
      containers:
        - name: postgres
          image: ankane/pgvector:v0.5.1
          ports:
            - containerPort: 5432
          imagePullPolicy: IfNotPresent
          resources: {}
          envFrom:
            - configMapRef:
                name: insecure-psql-config
          volumeMounts:
            - mountPath: "/var/lib/postgresql/data"
              name: postgresql-storage
      volumes:
        - name: postgresql-storage
          persistentVolumeClaim:
            claimName: placeholder-postgresql-persistent-volume-claim
