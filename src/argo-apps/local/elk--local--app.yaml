apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: elk--local--app
  namespace: argocd
spec:
  destination:
    namespace: ssp--elk-local
    server: https://kubernetes.default.svc
  project: default
  source:
    path: src/app-groups/elk/scripts/local
    repoURL: https://github.com/balazskrizsan/argo_ubuntu
    targetRevision: master
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
    syncOptions:
      - CreateNamespace=true
