apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argo-cd--local--app
  namespace: argocd
spec:
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc
  project: default
  source:
    path: src/app-groups/argo-cd/scripts/local
    repoURL: https://github.com/balazskrizsan/argo_ubuntu
    targetRevision: master
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
